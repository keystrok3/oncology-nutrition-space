# Oncology Nutrition Space Website

## Admin access

Visit `/admin/login` for normal sign-in.

If the system has no users yet, visit `/admin/setup` to create the first admin account. After that, additional admins can be created from `/admin/users` by an existing admin.
